import ClientsPage from "@/pages/app/clients/page"

// Vendors page reuses the Clients page component — it adapts based on activeMode
export default function VendorsPage() {
  return <ClientsPage />
}
